stages:
  - build
  - test

build:
  stage: build
  script:
  - ./gradlew build
  - echo "you built the app"

  artifacts:
    paths:
      - build/libs/g-hello-0.0.1-SNAPSHOT.jar

test-job1:
  stage: test
  script:
    - echo "This job tests something"
    - ./gradlew test


